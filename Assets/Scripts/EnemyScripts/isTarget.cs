using UnityEngine;

public class isTarget : MonoBehaviour
{
    private void Start()
    {
        this.enabled = true;
    }
    //this is only used as a script type so enemies have something to find.
}
